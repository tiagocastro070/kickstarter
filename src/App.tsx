import { ThemeSwitcher } from "./containers";

function App() {
  return (
    <div className="App d-flex direction-column align-items-center justify-center">
      <h1>Kickstarter</h1>
      <p>
        Start by editing the files inside the <code>src</code> folder.
      </p>
      <ThemeSwitcher />
    </div>
  );
}

export default App;
